# my-blog

마크다운 파일을 읽어서 정적 블로그 웹사이트로 변환하는 프로젝트.

## 핵심 원칙

- **프레임워크 없음**: React, Vue, Svelte 등 프레임워크나 번들러(Webpack, Vite 등) 사용 금지. 순수 HTML, CSS, JavaScript(바닐라 JS)만 사용한다.
- **빌드 스텝 없음**: 브라우저에서 바로 열리거나 간단한 정적 서버로 서빙 가능해야 한다. npm 빌드 파이프라인을 요구하지 않는다.
- 외부 CSS/JS 프레임워크(Bootstrap, Tailwind CDN 등)도 지양한다. 필요한 스타일은 직접 작성한다.
- 마크다운 파싱 라이브러리가 꼭 필요하면 의존성을 최소화하고, 가능하면 간단한 자체 파서나 단일 파일 라이브러리를 CDN 없이 로컬에 포함해 사용한다. 새 의존성을 추가하기 전에는 먼저 사용자와 상의한다.

## 디자인 방향

- 깔끔하고 읽기 좋은 타이포그래피 중심 디자인. 본문 가독성이 최우선.
- **다크 모드 지원 필수**: `prefers-color-scheme`을 기본으로 따르되, 사용자가 수동으로 토글할 수 있게 하고 선택값은 `localStorage`에 저장한다.
- **모바일 반응형 필수**: 뷰포트 메타 태그, 상대 단위(rem/%), 미디어 쿼리로 작은 화면에서도 레이아웃이 깨지지 않게 한다.
- 색상, 여백, 폰트 크기는 CSS 변수(`:root`)로 관리해 라이트/다크 테마 전환이 쉽게 되도록 한다.
- 과한 애니메이션이나 장식 요소보다 콘텐츠 가독성을 우선한다.

## 아키텍처 방향

- 마크다운 파일(`posts/` 또는 유사 디렉토리)을 읽어 HTML로 변환해 렌더링한다.
- 정적 사이트 생성(빌드 시 HTML 파일 생성) 방식과, 클라이언트에서 마크다운을 fetch 후 JS로 파싱해 렌더링하는 방식 중 아직 미정 — 새 기능을 추가하기 전에 이 구조가 정해져 있는지 확인하고, 없다면 사용자와 상의한다.
- 게시물 목록, 개별 게시물 페이지, (선택) 태그/카테고리 정도의 최소 기능부터 시작한다.

## 코드 스타일

- 불필요한 주석 금지. 코드 자체로 의도가 드러나지 않는 비직관적인 부분에만 짧게 주석을 단다.
- 요청 범위를 벗어난 리팩터링이나 추상화를 미리 만들지 않는다.
